{
  "name": "sih26001-work-b-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Express + PostgreSQL backend for SIH26001 (NER Landslide Early Warning System). Serves the existing React/Vite frontend and is ready to receive real ML predictions later.",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "node --watch src/app.js",
    "db:schema": "psql \"$DATABASE_URL\" -f src/db/schema.sql",
    "db:seed": "psql \"$DATABASE_URL\" -f src/db/seed.sql"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
